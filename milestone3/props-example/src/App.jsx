import Button from "./components/Button"

const App = () => {
  return (
    <div>
      <Button label={'Light'} className={'default'} />
      <Button label={'Dark'} className={'dark'} />
    </div>
  )
}

export default App
